/**
 * 用户管理模块（向后兼容导出）
 *
 * 注意：此文件保留用于向后兼容
 * 新代码请直接导入 services/authService.js 和 services/galleryService.js
 */

export { AuthService } from './services/authService.js';
export { GalleryService } from './services/galleryService.js';
